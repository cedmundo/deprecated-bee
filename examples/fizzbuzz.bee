def fizzBuzz(n) =
  if n % 15 == 0 then
    "FizzBuzz"
  elif n % 5 == 0 then
    "Fizz"
  elif n % 3 == 0 then
    "Buzz"
  else
    ""

def main() =
  puts(fizzBuzz(n)) for n in [1, 2, 3] if fizzBuzz(n) != ""
